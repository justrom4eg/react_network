import React from 'react'

function Header() {
    return (
      <div>
        <a href="#">HOME</a>
        <a href="#">MESSAGES</a>
        <a href="#">ABOUT</a>
        <a href="#">SETTINGS</a>
      </div>
    );
  }

  export default Header